apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "htcondor-chart.fullname" . }}-test-connection"
  labels:
    {{- include "htcondor-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "htcondor-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
